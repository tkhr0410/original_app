<% if signed_in? %>
  <div class="row">
    <aside class="col-md-4">
      <section>
        <%= render 'shared/user_info' %>
      </section>
      <section>
        <%= render 'shared/stats' %>
      </section>
      <section>
        <%= render 'shared/micropost_form' %>
      </section>
    </aside>
    <div class="col-md-8">
      <h3>Micropost Feed</h3>
      <%= render 'shared/feed' %>
    </div>
  </div>
<% else %>
<div class="center jumbotron">
  <h1>sky</h1>
  <h2>
  skyは空撮と観光を繋げるアプリケーションです。  
  </h2>

  <%= link_to "Sign up now!", new_user_registration_path, class: "btn btn-lg btn-danger" %>
</div>
<% end %>
